# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # Increased timeout for long-running AI content generation tasks
  timeoutSeconds: 120
  # Ensure enough memory for Genkit and content processing
  memory: 1024Mi
  # Secrets must be explicitly mapped to environment variables to be accessible in code
  secrets:
    - variable: GOOGLE_GENAI_API_KEY
      secret: GOOGLE_GENAI_API_KEY